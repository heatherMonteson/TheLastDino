package src;

public class Enums {
    public enum Event {LostLife, AteLeaves, LevelCompleted}
    public enum GamePiece{Dino, Leaf, Bush, Snowball, Fireball, Icicle, Cloud, SmokeCloud}
    public enum Level {L1, L2, L3}
    public enum Popup {Signup, Instructions, GameOver}
}
