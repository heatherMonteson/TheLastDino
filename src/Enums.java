package src;
/*
 * Enums: Use to classify events and objects in the system
 */
public class Enums {
    public enum Event {LostLife, AteLeaves, LevelCompleted, PlayerDied}
    public enum GamePiece{Dino, Leaf, Bush, Snowball, Fireball, Icicle, Cloud, SmokeCloud}
    public enum Level {L1, L2, L3, End}
    public enum Popup {Signup, Instructions}
}
